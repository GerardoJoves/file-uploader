doctype html
html
  head
    title= title || 'File Uploader'
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width' initial-scale=1)
  body
    aside
      nav
        ul
          li: a(href='/home') Home
    block content
