extends ../layouts/default.pug

block content
  main
    form(method='POST')
      h1= title
      label(for='folderName') Folder name
      input(id='falderNam' type='text' name='folderName')
      button(type='submit') Create
